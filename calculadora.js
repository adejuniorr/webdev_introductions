function displaynum(n1){
    Calculadora.text1.value = Calculadora.text1.value + n1
}

/*
Com relação aos parâmetros que podem ser estabelecidos dentro de uma função,
nota-se que as funções que não os possuem são aquelas que determinam um bloco 
específico para realizar um procedimento imutável e permanente.

Por outro lado, as funções que carregam parâmetros dentro de si, permitem 
execuções mais variadas visto que os parâmetros podem ser alterados de acordo 
com as inúmeras chamadas de uma mesma função.
*/